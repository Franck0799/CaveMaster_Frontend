//// dashboard 2///

import React, { useState, useEffect } from 'react';
import { Wine, MapPin, Heart, ShoppingCart, User, Search, Star, Award, Calendar, Book, Home, Menu, X, ChevronRight, Filter, TrendingUp, Gift } from 'lucide-react';

// Données mockées pour la démo
const mockWines = [
  { id: 1, name: 'Château Margaux 2015', region: 'Bordeaux', price: 450, image: '🍷', rating: 4.8, type: 'Rouge', grape: 'Cabernet Sauvignon', year: 2015, cave: 'Cave Prestige Paris' },
  { id: 2, name: 'Domaine de la Romanée-Conti', region: 'Bourgogne', price: 1200, image: '🍷', rating: 5.0, type: 'Rouge', grape: 'Pinot Noir', year: 2016, cave: 'Cave d\'Excellence' },
  { id: 3, name: 'Champagne Dom Pérignon', region: 'Champagne', price: 180, image: '🥂', rating: 4.9, type: 'Pétillant', grape: 'Chardonnay', year: 2012, cave: 'Cave Prestige Paris' },
  { id: 4, name: 'Châteauneuf-du-Pape', region: 'Rhône', price: 85, image: '🍷', rating: 4.6, type: 'Rouge', grape: 'Grenache', year: 2018, cave: 'Cave du Sud' },
  { id: 5, name: 'Sancerre Blanc', region: 'Loire', price: 32, image: '🍾', rating: 4.4, type: 'Blanc', grape: 'Sauvignon Blanc', year: 2020, cave: 'Cave de Loire' },
  { id: 6, name: 'Pouilly-Fuissé', region: 'Bourgogne', price: 45, image: '🍾', rating: 4.5, type: 'Blanc', grape: 'Chardonnay', year: 2019, cave: 'Cave d\'Excellence' },
];

const mockCaves = [
  { id: 1, name: 'Cave Prestige Paris', location: 'Paris 8ème', wines: 250, rating: 4.7, image: '🏛️', specialty: 'Grands Crus' },
  { id: 2, name: 'Cave d\'Excellence', location: 'Lyon', wines: 180, rating: 4.8, image: '🏛️', specialty: 'Bourgogne' },
  { id: 3, name: 'Cave du Sud', location: 'Marseille', wines: 120, rating: 4.5, image: '🏛️', specialty: 'Rhône & Provence' },
  { id: 4, name: 'Cave de Loire', location: 'Tours', wines: 95, rating: 4.6, image: '🏛️', specialty: 'Vins de Loire' },
];

const mockOrders = [
  { id: 1, date: '2025-10-15', status: 'Livrée', total: 285, items: 3 },
  { id: 2, date: '2025-10-28', status: 'En cours', total: 450, items: 1 },
  { id: 3, date: '2025-11-05', status: 'Préparation', total: 164, items: 2 },
];

const App = () => {
  const [currentPage, setCurrentPage] = useState('home');
  const [cart, setCart] = useState([]);
  const [favorites, setFavorites] = useState([]);
  const [selectedWine, setSelectedWine] = useState(null);
  const [menuOpen, setMenuOpen] = useState(false);
  const [searchQuery, setSearchQuery] = useState('');
  const [filters, setFilters] = useState({ type: 'all', region: 'all', priceRange: 'all' });
  const [loyaltyPoints, setLoyaltyPoints] = useState(1250);

  // Navigation entre les pages
  const navigate = (page, data = null) => {
    setCurrentPage(page);
    if (page === 'wine-detail' && data) {
      setSelectedWine(data);
    }
    setMenuOpen(false);
    window.scrollTo(0, 0);
  };

  // Gestion du panier
  const addToCart = (wine) => {
    setCart([...cart, { ...wine, cartId: Date.now() }]);
  };

  const removeFromCart = (cartId) => {
    setCart(cart.filter(item => item.cartId !== cartId));
  };

  // Gestion des favoris
  const toggleFavorite = (wine) => {
    const isFav = favorites.some(f => f.id === wine.id);
    if (isFav) {
      setFavorites(favorites.filter(f => f.id !== wine.id));
    } else {
      setFavorites([...favorites, wine]);
    }
  };

  const isFavorite = (wineId) => favorites.some(f => f.id === wineId);

  // Filtrage des vins
  const getFilteredWines = () => {
    return mockWines.filter(wine => {
      const matchSearch = wine.name.toLowerCase().includes(searchQuery.toLowerCase()) ||
                         wine.region.toLowerCase().includes(searchQuery.toLowerCase());
      const matchType = filters.type === 'all' || wine.type === filters.type;
      const matchRegion = filters.region === 'all' || wine.region === filters.region;
      const matchPrice = filters.priceRange === 'all' ||
                        (filters.priceRange === 'low' && wine.price < 50) ||
                        (filters.priceRange === 'mid' && wine.price >= 50 && wine.price < 200) ||
                        (filters.priceRange === 'high' && wine.price >= 200);
      return matchSearch && matchType && matchRegion && matchPrice;
    });
  };

  // Navigation menu
  const NavMenu = () => (
    <nav className={`fixed top-0 right-0 h-full w-80 bg-gradient-to-br from-gray-900 to-gray-800 text-white transform transition-transform duration-300 z-50 ${menuOpen ? 'translate-x-0' : 'translate-x-full'}`}>
      <div className="p-6">
        <button onClick={() => setMenuOpen(false)} className="absolute top-4 right-4 p-2 hover:bg-gray-700 rounded-lg">
          <X size={24} />
        </button>
        <h2 className="text-2xl font-bold mb-8 mt-8">Menu</h2>
        <div className="space-y-2">
          {[
            { page: 'home', icon: Home, label: 'Accueil' },
            { page: 'caves', icon: MapPin, label: 'Caves partenaires' },
            { page: 'catalog', icon: Wine, label: 'Catalogue' },
            { page: 'my-orders', icon: ShoppingCart, label: 'Mes commandes' },
            { page: 'reservations', icon: Calendar, label: 'Réservations' },
            { page: 'favorites', icon: Heart, label: 'Mes favoris' },
            { page: 'wine-advice', icon: Book, label: 'Conseils' },
            { page: 'loyalty', icon: Award, label: 'Fidélité' },
            { page: 'profile', icon: User, label: 'Mon compte' },
          ].map(({ page, icon: Icon, label }) => (
            <button
              key={page}
              onClick={() => navigate(page)}
              className="w-full flex items-center space-x-3 p-3 hover:bg-gray-700 rounded-lg transition"
            >
              <Icon size={20} />
              <span>{label}</span>
            </button>
          ))}
        </div>
      </div>
    </nav>
  );

  // Header
  const Header = () => (
    <header className="sticky top-0 bg-gradient-to-r from-purple-900 to-red-900 text-white shadow-xl z-40">
      <div className="max-w-7xl mx-auto px-4 py-4">
        <div className="flex items-center justify-between">
          <button onClick={() => navigate('home')} className="flex items-center space-x-2 hover:opacity-80 transition">
            <Wine size={32} className="text-amber-300" />
            <span className="text-2xl font-bold">VinothèqueⓇ</span>
          </button>
          <div className="flex items-center space-x-4">
            <button onClick={() => navigate('favorites')} className="relative p-2 hover:bg-white/20 rounded-lg transition">
              <Heart size={24} fill={favorites.length > 0 ? 'currentColor' : 'none'} />
              {favorites.length > 0 && (
                <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                  {favorites.length}
                </span>
              )}
            </button>
            <button onClick={() => navigate('cart')} className="relative p-2 hover:bg-white/20 rounded-lg transition">
              <ShoppingCart size={24} />
              {cart.length > 0 && (
                <span className="absolute -top-1 -right-1 bg-amber-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                  {cart.length}
                </span>
              )}
            </button>
            <button onClick={() => setMenuOpen(true)} className="p-2 hover:bg-white/20 rounded-lg transition">
              <Menu size={24} />
            </button>
          </div>
        </div>
      </div>
    </header>
  );

  // Page: Home
  const HomePage = () => (
    <div className="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100">
      {/* Hero Section */}
      <section className="relative bg-gradient-to-r from-purple-900 via-red-900 to-purple-900 text-white py-24">
        <div className="max-w-7xl mx-auto px-4 text-center">
          <h1 className="text-5xl md:text-6xl font-bold mb-6 animate-fade-in">Découvrez l'Excellence du Vin</h1>
          <p className="text-xl md:text-2xl mb-8 text-purple-200">Les meilleures caves et les meilleurs crus à portée de clic</p>
          <button onClick={() => navigate('catalog')} className="bg-amber-500 hover:bg-amber-600 text-white px-8 py-4 rounded-full text-lg font-semibold transition transform hover:scale-105 shadow-lg">
            Explorer le Catalogue
          </button>
        </div>
      </section>

      {/* Promotions */}
      <section className="max-w-7xl mx-auto px-4 py-12">
        <div className="flex items-center justify-between mb-6">
          <h2 className="text-3xl font-bold text-gray-800 flex items-center gap-2">
            <TrendingUp className="text-amber-600" />
            Promotions du Moment
          </h2>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          {mockWines.slice(0, 3).map(wine => (
            <div key={wine.id} className="bg-white rounded-2xl shadow-lg overflow-hidden transform hover:scale-105 transition cursor-pointer" onClick={() => navigate('wine-detail', wine)}>
              <div className="bg-gradient-to-br from-purple-100 to-red-100 p-8 flex justify-center items-center h-48">
                <span className="text-8xl">{wine.image}</span>
              </div>
              <div className="p-6">
                <div className="flex justify-between items-start mb-2">
                  <h3 className="font-bold text-lg text-gray-800">{wine.name}</h3>
                  <button onClick={(e) => { e.stopPropagation(); toggleFavorite(wine); }} className="p-2 hover:bg-gray-100 rounded-full">
                    <Heart size={20} fill={isFavorite(wine.id) ? '#ef4444' : 'none'} className={isFavorite(wine.id) ? 'text-red-500' : 'text-gray-400'} />
                  </button>
                </div>
                <p className="text-gray-600 text-sm mb-2">{wine.region} • {wine.year}</p>
                <div className="flex items-center justify-between">
                  <span className="text-2xl font-bold text-purple-900">{wine.price}€</span>
                  <div className="flex items-center text-amber-500">
                    <Star size={16} fill="currentColor" />
                    <span className="ml-1 text-sm">{wine.rating}</span>
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* Caves Partenaires */}
      <section className="max-w-7xl mx-auto px-4 py-12">
        <h2 className="text-3xl font-bold text-gray-800 mb-6 flex items-center gap-2">
          <MapPin className="text-purple-600" />
          Caves Partenaires
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-4 gap-6">
          {mockCaves.map(cave => (
            <div key={cave.id} className="bg-white rounded-xl shadow-md p-6 hover:shadow-xl transition cursor-pointer" onClick={() => navigate('caves')}>
              <div className="text-6xl mb-4 text-center">{cave.image}</div>
              <h3 className="font-bold text-lg mb-2 text-center">{cave.name}</h3>
              <p className="text-gray-600 text-sm text-center mb-2">{cave.location}</p>
              <p className="text-amber-600 text-sm text-center font-semibold">{cave.specialty}</p>
              <div className="flex items-center justify-center mt-3 text-gray-600 text-sm">
                <Wine size={14} className="mr-1" />
                <span>{cave.wines} vins</span>
              </div>
            </div>
          ))}
        </div>
        <div className="text-center mt-8">
          <button onClick={() => navigate('caves')} className="text-purple-700 font-semibold hover:text-purple-900 flex items-center gap-2 mx-auto">
            Voir toutes les caves <ChevronRight size={20} />
          </button>
        </div>
      </section>

      {/* Nouveautés */}
      <section className="bg-gradient-to-r from-purple-50 to-red-50 py-12">
        <div className="max-w-7xl mx-auto px-4">
          <h2 className="text-3xl font-bold text-gray-800 mb-6 flex items-center gap-2">
            <Gift className="text-red-600" />
            Nouveautés
          </h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {mockWines.slice(3, 6).map(wine => (
              <div key={wine.id} className="bg-white rounded-xl shadow-md p-6 hover:shadow-xl transition cursor-pointer" onClick={() => navigate('wine-detail', wine)}>
                <div className="text-7xl mb-4 text-center">{wine.image}</div>
                <h3 className="font-bold text-lg mb-2">{wine.name}</h3>
                <p className="text-gray-600 text-sm mb-3">{wine.region}</p>
                <div className="flex justify-between items-center">
                  <span className="text-xl font-bold text-purple-900">{wine.price}€</span>
                  <button onClick={(e) => { e.stopPropagation(); addToCart(wine); }} className="bg-amber-500 hover:bg-amber-600 text-white px-4 py-2 rounded-lg text-sm font-semibold transition">
                    Ajouter
                  </button>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>
    </div>
  );

  // Page: Caves
  const CavesPage = () => (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-7xl mx-auto px-4">
        <h1 className="text-4xl font-bold text-gray-800 mb-2">Caves Partenaires</h1>
        <p className="text-gray-600 mb-8">Découvrez nos caves d'exception à travers toute la France</p>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          {mockCaves.map(cave => (
            <div key={cave.id} className="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-2xl transition">
              <div className="bg-gradient-to-r from-purple-600 to-red-600 p-12 flex justify-center items-center">
                <span className="text-9xl">{cave.image}</span>
              </div>
              <div className="p-6">
                <h3 className="text-2xl font-bold mb-2">{cave.name}</h3>
                <div className="flex items-center text-gray-600 mb-3">
                  <MapPin size={16} className="mr-2" />
                  <span>{cave.location}</span>
                </div>
                <div className="flex items-center justify-between mb-4">
                  <div className="flex items-center text-amber-500">
                    <Star size={20} fill="currentColor" className="mr-1" />
                    <span className="font-bold">{cave.rating}</span>
                  </div>
                  <div className="flex items-center text-gray-600">
                    <Wine size={18} className="mr-2" />
                    <span className="font-semibold">{cave.wines} vins</span>
                  </div>
                </div>
                <div className="bg-amber-50 rounded-lg p-3 mb-4">
                  <p className="text-sm text-amber-800 font-semibold">Spécialité: {cave.specialty}</p>
                </div>
                <button onClick={() => navigate('catalog')} className="w-full bg-purple-600 hover:bg-purple-700 text-white py-3 rounded-lg font-semibold transition">
                  Voir les vins
                </button>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );

  // Page: Catalog
  const CatalogPage = () => (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-7xl mx-auto px-4">
        <h1 className="text-4xl font-bold text-gray-800 mb-8">Catalogue des Vins</h1>

        {/* Barre de recherche et filtres */}
        <div className="bg-white rounded-xl shadow-md p-6 mb-8">
          <div className="flex flex-col md:flex-row gap-4">
            <div className="flex-1 relative">
              <Search className="absolute left-3 top-3 text-gray-400" size={20} />
              <input
                type="text"
                placeholder="Rechercher un vin, une région..."
                value={searchQuery}
                onChange={(e) => setSearchQuery(e.target.value)}
                className="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500"
              />
            </div>
            <select
              value={filters.type}
              onChange={(e) => setFilters({ ...filters, type: e.target.value })}
              className="px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500"
            >
              <option value="all">Tous les types</option>
              <option value="Rouge">Rouge</option>
              <option value="Blanc">Blanc</option>
              <option value="Pétillant">Pétillant</option>
            </select>
            <select
              value={filters.region}
              onChange={(e) => setFilters({ ...filters, region: e.target.value })}
              className="px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500"
            >
              <option value="all">Toutes les régions</option>
              <option value="Bordeaux">Bordeaux</option>
              <option value="Bourgogne">Bourgogne</option>
              <option value="Champagne">Champagne</option>
              <option value="Rhône">Rhône</option>
              <option value="Loire">Loire</option>
            </select>
            <select
              value={filters.priceRange}
              onChange={(e) => setFilters({ ...filters, priceRange: e.target.value })}
              className="px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500"
            >
              <option value="all">Tous les prix</option>
              <option value="low">Moins de 50€</option>
              <option value="mid">50€ - 200€</option>
              <option value="high">Plus de 200€</option>
            </select>
          </div>
        </div>

        {/* Liste des vins */}
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          {getFilteredWines().map(wine => (
            <div key={wine.id} className="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-2xl transition transform hover:scale-105 cursor-pointer" onClick={() => navigate('wine-detail', wine)}>
              <div className="bg-gradient-to-br from-purple-100 to-red-100 p-8 flex justify-center items-center h-56 relative">
                <span className="text-9xl">{wine.image}</span>
                <button
                  onClick={(e) => { e.stopPropagation(); toggleFavorite(wine); }}
                  className="absolute top-4 right-4 p-2 bg-white rounded-full shadow-md hover:shadow-lg transition"
                >
                  <Heart size={20} fill={isFavorite(wine.id) ? '#ef4444' : 'none'} className={isFavorite(wine.id) ? 'text-red-500' : 'text-gray-400'} />
                </button>
              </div>
              <div className="p-6">
                <h3 className="font-bold text-xl mb-2">{wine.name}</h3>
                <p className="text-gray-600 text-sm mb-3">{wine.region} • {wine.grape} • {wine.year}</p>
                <div className="flex items-center mb-4">
                  <Star size={16} fill="#f59e0b" className="text-amber-500 mr-1" />
                  <span className="text-sm font-semibold">{wine.rating}</span>
                  <span className="text-gray-400 text-sm ml-2">• {wine.cave}</span>
                </div>
                <div className="flex items-center justify-between">
                  <span className="text-2xl font-bold text-purple-900">{wine.price}€</span>
                  <button
                    onClick={(e) => { e.stopPropagation(); addToCart(wine); }}
                    className="bg-amber-500 hover:bg-amber-600 text-white px-6 py-2 rounded-lg font-semibold transition"
                  >
                    Ajouter
                  </button>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );

  // Page: Wine Detail
  const WineDetailPage = () => {
    if (!selectedWine) return <div>Vin non trouvé</div>;

    return (
      <div className="min-h-screen bg-gray-50 py-8">
        <div className="max-w-6xl mx-auto px-4">
          <button onClick={() => navigate('catalog')} className="mb-6 text-purple-700 hover:text-purple-900 font-semibold flex items-center gap-2">
            ← Retour au catalogue
          </button>

          <div className="bg-white rounded-2xl shadow-2xl overflow-hidden">
            <div className="grid md:grid-cols-2 gap-8">
              <div className="bg-gradient-to-br from-purple-100 to-red-100 p-16 flex justify-center items-center">
                <span className="text-[200px]">{selectedWine.image}</span>
              </div>

              <div className="p-8">
                <div className="flex justify-between items-start mb-4">
                  <h1 className="text-4xl font-bold text-gray-800">{selectedWine.name}</h1>
                  <button onClick={() => toggleFavorite(selectedWine)} className="p-3 bg-gray-100 rounded-full hover:bg-gray-200 transition">
                    <Heart size={28} fill={isFavorite(selectedWine.id) ? '#ef4444' : 'none'} className={isFavorite(selectedWine.id) ? 'text-red-500' : 'text-gray-400'} />
                  </button>
                </div>

                <div className="flex items-center mb-6">
                  <Star size={24} fill="#f59e0b" className="text-amber-500 mr-2" />
                  <span className="text-2xl font-bold">{selectedWine.rating}</span>
                  <span className="text-gray-500 ml-2">/5.0</span>
                </div>

                <div className="space-y-4 mb-8">
                  <div className="flex items-center gap-3">
                    <span className="bg-purple-100 text-purple-800 px-4 py-2 rounded-lg font-semibold">{selectedWine.type}</span>
                    <span className="bg-amber-100 text-amber-800 px-4 py-2 rounded-lg font-semibold">{selectedWine.year}</span>
                  </div>

                  <div className="grid grid-cols-2 gap-4">
                    <div className="bg-gray-50 p-4 rounded-lg">
                      <p className="text-gray-600 text-sm mb-1">Région</p>
                      <p className="font-bold text-gray-800">{selectedWine.region}</p>
                    </div>
                    <div className="bg-gray-50 p-4 rounded-lg">
                      <p className="text-gray-600 text-sm mb-1">Cépage</p>
                      <p className="font-bold text-gray-800">{selectedWine.grape}</p>
                    </div>
                  </div>

                  <div className="bg-purple-50 p-4 rounded-lg">
                    <p className="text-gray-600 text-sm mb-1">Disponible chez</p>
                    <p className="font-bold text-purple-800">{selectedWine.cave}</p>
                  </div>
                </div>

                <div className="bg-gradient-to-r from-purple-50 to-red-50 p-6 rounded-xl mb-6">
                  <h3 className="font-bold text-lg mb-3">Accords Recommandés</h3>
                  <div className="flex flex-wrap gap-2">
                    <span className="bg-white px-4 py-2 rounded-lg text-sm">🥩 Viandes rouges</span>
                    <span className="bg-white px-4 py-2 rounded-lg text-sm">🧀 Fromages affinés</span>
                    <span className="bg-white px-4 py-2 rounded-lg text-sm">🍄 Gibier</span>
                  </div>
                </div>

                <div className="flex items-center justify-between pt-6 border-t">
                  <div>
                    <p className="text-gray-600 text-sm mb-1">Prix</p>
                    <p className="text-4xl font-bold text-purple-900">{selectedWine.price}€</p>
                  </div>
                  <button onClick={() => { addToCart(selectedWine); navigate('cart'); }} className="bg-amber-500 hover:bg-amber-600 text-white px-8 py-4 rounded-xl font-bold text-lg transition transform hover:scale-105 shadow-lg">
                    Ajouter au panier
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    );
  };

  // Page: My Orders
  const MyOrdersPage = () => (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-5xl mx-auto px-4">
        <h1 className="text-4xl font-bold text-gray-800 mb-8">Mes Commandes</h1>

        <div className="space-y-4">
          {mockOrders.map(order => (
            <div key={order.id} className="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
              <div className="flex justify-between items-start mb-4">
                <div>
                  <h3 className="text-xl font-bold text-gray-800">Commande #{order.id}</h3>
                  <p className="text-gray-600 text-sm">{order.date}</p>
                </div>
                <span className={`px-4 py-2 rounded-full text-sm font-semibold ${
                  order.status === 'Livrée' ? 'bg-green-100 text-green-800' :
                  order.status === 'En cours' ? 'bg-blue-100 text-blue-800' :
                  'bg-amber-100 text-amber-800'
                }`}>
                  {order.status}
                </span>
              </div>
              <div className="flex justify-between items-center pt-4 border-t">
                <div>
                  <p className="text-gray-600 text-sm">Articles: {order.items}</p>
                  <p className="text-2xl font-bold text-purple-900">{order.total}€</p>
                </div>
                <button className="bg-purple-600 hover:bg-purple-700 text-white px-6 py-2 rounded-lg font-semibold transition">
                  Voir détails
                </button>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );

  // Page: Reservations
  const ReservationsPage = () => {
    const [reservations, setReservations] = useState([
      { id: 1, cave: 'Cave Prestige Paris', date: '2025-11-15', time: '19:00', guests: 4, status: 'Confirmée' },
      { id: 2, cave: 'Cave d\'Excellence', date: '2025-11-22', time: '20:00', guests: 2, status: 'En attente' },
    ]);

    return (
      <div className="min-h-screen bg-gray-50 py-8">
        <div className="max-w-5xl mx-auto px-4">
          <div className="flex justify-between items-center mb-8">
            <h1 className="text-4xl font-bold text-gray-800">Mes Réservations</h1>
            <button className="bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-lg font-semibold transition flex items-center gap-2">
              <Calendar size={20} />
              Nouvelle réservation
            </button>
          </div>

          <div className="space-y-4">
            {reservations.map(res => (
              <div key={res.id} className="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                <div className="flex justify-between items-start mb-4">
                  <div>
                    <h3 className="text-xl font-bold text-gray-800">{res.cave}</h3>
                    <div className="flex items-center gap-4 mt-2 text-gray-600">
                      <span className="flex items-center gap-1">
                        <Calendar size={16} />
                        {res.date}
                      </span>
                      <span>🕐 {res.time}</span>
                      <span>👥 {res.guests} personnes</span>
                    </div>
                  </div>
                  <span className={`px-4 py-2 rounded-full text-sm font-semibold ${
                    res.status === 'Confirmée' ? 'bg-green-100 text-green-800' : 'bg-amber-100 text-amber-800'
                  }`}>
                    {res.status}
                  </span>
                </div>
                <div className="flex gap-3 pt-4 border-t">
                  <button className="flex-1 bg-purple-100 hover:bg-purple-200 text-purple-800 py-2 rounded-lg font-semibold transition">
                    Modifier
                  </button>
                  <button className="flex-1 bg-red-100 hover:bg-red-200 text-red-800 py-2 rounded-lg font-semibold transition">
                    Annuler
                  </button>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    );
  };

  // Page: Favorites
  const FavoritesPage = () => (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-7xl mx-auto px-4">
        <h1 className="text-4xl font-bold text-gray-800 mb-8 flex items-center gap-3">
          <Heart className="text-red-500" fill="currentColor" />
          Mes Favoris
        </h1>

        {favorites.length === 0 ? (
          <div className="bg-white rounded-xl shadow-md p-12 text-center">
            <Heart size={64} className="text-gray-300 mx-auto mb-4" />
            <h3 className="text-xl font-bold text-gray-800 mb-2">Aucun favori pour le moment</h3>
            <p className="text-gray-600 mb-6">Ajoutez des vins à vos favoris pour les retrouver facilement</p>
            <button onClick={() => navigate('catalog')} className="bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-lg font-semibold transition">
              Explorer le catalogue
            </button>
          </div>
        ) : (
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {favorites.map(wine => (
              <div key={wine.id} className="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-2xl transition cursor-pointer" onClick={() => navigate('wine-detail', wine)}>
                <div className="bg-gradient-to-br from-purple-100 to-red-100 p-8 flex justify-center items-center h-56 relative">
                  <span className="text-9xl">{wine.image}</span>
                  <button
                    onClick={(e) => { e.stopPropagation(); toggleFavorite(wine); }}
                    className="absolute top-4 right-4 p-2 bg-white rounded-full shadow-md"
                  >
                    <Heart size={20} fill="#ef4444" className="text-red-500" />
                  </button>
                </div>
                <div className="p-6">
                  <h3 className="font-bold text-xl mb-2">{wine.name}</h3>
                  <p className="text-gray-600 text-sm mb-3">{wine.region} • {wine.year}</p>
                  <div className="flex items-center justify-between">
                    <span className="text-2xl font-bold text-purple-900">{wine.price}€</span>
                    <button
                      onClick={(e) => { e.stopPropagation(); addToCart(wine); }}
                      className="bg-amber-500 hover:bg-amber-600 text-white px-6 py-2 rounded-lg font-semibold transition"
                    >
                      Ajouter
                    </button>
                  </div>
                </div>
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  );

  // Page: Wine Advice
  const WineAdvicePage = () => (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-5xl mx-auto px-4">
        <h1 className="text-4xl font-bold text-gray-800 mb-2">Conseils & Recommandations</h1>
        <p className="text-gray-600 mb-8">Découvrez les vins qui correspondent à vos goûts</p>

        {/* Quiz Section */}
        <div className="bg-gradient-to-r from-purple-600 to-red-600 rounded-2xl shadow-xl p-8 text-white mb-8">
          <div className="flex items-center justify-between">
            <div>
              <h2 className="text-3xl font-bold mb-3">Quiz Préférences</h2>
              <p className="text-purple-100 mb-4">Répondez à quelques questions pour recevoir des suggestions personnalisées</p>
              <button className="bg-white text-purple-700 px-6 py-3 rounded-lg font-bold hover:bg-gray-100 transition">
                Commencer le quiz
              </button>
            </div>
            <div className="text-9xl">🎯</div>
          </div>
        </div>

        {/* Suggestions */}
        <div className="mb-8">
          <h2 className="text-2xl font-bold text-gray-800 mb-6">Suggestions Personnalisées</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            {mockWines.slice(0, 4).map(wine => (
              <div key={wine.id} className="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition cursor-pointer" onClick={() => navigate('wine-detail', wine)}>
                <div className="flex gap-4">
                  <div className="bg-gradient-to-br from-purple-100 to-red-100 rounded-lg p-4 flex items-center justify-center">
                    <span className="text-6xl">{wine.image}</span>
                  </div>
                  <div className="flex-1">
                    <h3 className="font-bold text-lg mb-1">{wine.name}</h3>
                    <p className="text-gray-600 text-sm mb-2">{wine.region}</p>
                    <div className="flex items-center justify-between">
                      <span className="text-xl font-bold text-purple-900">{wine.price}€</span>
                      <div className="flex items-center text-amber-500">
                        <Star size={16} fill="currentColor" />
                        <span className="ml-1 text-sm">{wine.rating}</span>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Blog Articles */}
        <div>
          <h2 className="text-2xl font-bold text-gray-800 mb-6">Articles & Guides</h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {[
              { title: 'Comment choisir un vin pour un dîner', emoji: '🍽️' },
              { title: 'Les accords mets et vins parfaits', emoji: '👨‍🍳' },
              { title: 'Guide des régions viticoles', emoji: '🗺️' },
            ].map((article, idx) => (
              <div key={idx} className="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition cursor-pointer">
                <div className="text-6xl mb-4">{article.emoji}</div>
                <h3 className="font-bold text-lg mb-2">{article.title}</h3>
                <p className="text-gray-600 text-sm mb-4">Découvrez nos conseils d'experts pour sublimer vos expériences œnologiques...</p>
                <button className="text-purple-700 font-semibold hover:text-purple-900 flex items-center gap-1">
                  Lire l'article <ChevronRight size={16} />
                </button>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );

  // Page: Loyalty
  const LoyaltyPage = () => (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-5xl mx-auto px-4">
        <h1 className="text-4xl font-bold text-gray-800 mb-8">Programme Fidélité</h1>

        {/* Points Card */}
        <div className="bg-gradient-to-r from-amber-500 to-amber-600 rounded-2xl shadow-2xl p-8 text-white mb-8">
          <div className="flex items-center justify-between mb-6">
            <div>
              <p className="text-amber-100 text-sm mb-1">Vos points</p>
              <p className="text-5xl font-bold">{loyaltyPoints}</p>
            </div>
            <Award size={80} className="text-amber-200" />
          </div>
          <div className="bg-white/20 rounded-lg p-4">
            <p className="text-sm mb-2">Niveau: <span className="font-bold">Gold Member</span></p>
            <div className="bg-white/30 h-2 rounded-full overflow-hidden">
              <div className="bg-white h-full" style={{ width: '75%' }}></div>
            </div>
            <p className="text-xs mt-2 text-amber-100">Plus que 250 points pour atteindre le niveau Platinum</p>
          </div>
        </div>

        {/* Rewards */}
        <div className="mb-8">
          <h2 className="text-2xl font-bold text-gray-800 mb-6">Récompenses Disponibles</h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {[
              { title: 'Livraison gratuite', points: 500, emoji: '🚚' },
              { title: 'Réduction 10€', points: 750, emoji: '💰' },
              { title: 'Dégustation offerte', points: 1000, emoji: '🍷' },
            ].map((reward, idx) => (
              <div key={idx} className="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                <div className="text-6xl mb-4 text-center">{reward.emoji}</div>
                <h3 className="font-bold text-lg mb-2 text-center">{reward.title}</h3>
                <p className="text-center text-gray-600 mb-4">{reward.points} points</p>
                <button className={`w-full py-3 rounded-lg font-semibold transition ${
                  loyaltyPoints >= reward.points
                    ? 'bg-amber-500 hover:bg-amber-600 text-white'
                    : 'bg-gray-200 text-gray-500 cursor-not-allowed'
                }`}>
                  {loyaltyPoints >= reward.points ? 'Échanger' : 'Pas assez de points'}
                </button>
              </div>
            ))}
          </div>
        </div>

        {/* Exclusive Offers */}
        <div>
          <h2 className="text-2xl font-bold text-gray-800 mb-6">Offres Exclusives</h2>
          <div className="bg-gradient-to-r from-purple-50 to-red-50 rounded-xl p-6">
            <div className="space-y-4">
              {[
                { title: '20% sur votre prochaine commande', valid: '2025-11-30' },
                { title: 'Accès prioritaire aux nouveautés', valid: '2025-12-31' },
                { title: 'Invitation événement VIP', valid: '2025-11-15' },
              ].map((offer, idx) => (
                <div key={idx} className="bg-white rounded-lg p-4 flex justify-between items-center">
                  <div>
                    <h3 className="font-bold text-gray-800">{offer.title}</h3>
                    <p className="text-sm text-gray-600">Valide jusqu'au {offer.valid}</p>
                  </div>
                  <button className="bg-purple-600 hover:bg-purple-700 text-white px-4 py-2 rounded-lg font-semibold transition text-sm">
                    Utiliser
                  </button>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  );

  // Page: Profile
  const ProfilePage = () => (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-4xl mx-auto px-4">
        <h1 className="text-4xl font-bold text-gray-800 mb-8">Mon Compte</h1>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
          <div className="bg-white rounded-xl shadow-md p-6 text-center">
            <div className="bg-purple-100 w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-4">
              <User size={40} className="text-purple-600" />
            </div>
            <h3 className="font-bold text-lg">Jean Dupont</h3>
            <p className="text-gray-600 text-sm">jean.dupont@email.com</p>
          </div>
          <div className="bg-white rounded-xl shadow-md p-6 text-center">
            <div className="bg-amber-100 w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-4">
              <Award size={40} className="text-amber-600" />
            </div>
            <h3 className="font-bold text-lg">{loyaltyPoints} points</h3>
            <p className="text-gray-600 text-sm">Niveau Gold</p>
          </div>
          <div className="bg-white rounded-xl shadow-md p-6 text-center">
            <div className="bg-green-100 w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-4">
              <ShoppingCart size={40} className="text-green-600" />
            </div>
            <h3 className="font-bold text-lg">{mockOrders.length} commandes</h3>
            <p className="text-gray-600 text-sm">Cette année</p>
          </div>
        </div>

        <div className="bg-white rounded-xl shadow-md p-6 mb-6">
          <h2 className="text-xl font-bold text-gray-800 mb-4">Informations Personnelles</h2>
          <div className="space-y-4">
            <div>
              <label className="block text-sm font-semibold text-gray-700 mb-2">Nom complet</label>
              <input type="text" value="Jean Dupont" className="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500" />
            </div>
            <div>
              <label className="block text-sm font-semibold text-gray-700 mb-2">Email</label>
              <input type="email" value="jean.dupont@email.com" className="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500" />
            </div>
            <div>
              <label className="block text-sm font-semibold text-gray-700 mb-2">Téléphone</label>
              <input type="tel" value="+33 6 12 34 56 78" className="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500" />
            </div>
            <button className="bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-lg font-semibold transition">
              Mettre à jour
            </button>
          </div>
        </div>

        <div className="bg-white rounded-xl shadow-md p-6">
          <h2 className="text-xl font-bold text-gray-800 mb-4">Préférences</h2>
          <div className="space-y-3">
            {[
              'Recevoir les newsletters',
              'Notifications des promotions',
              'Alertes nouveautés',
              'Recommandations personnalisées',
            ].map((pref, idx) => (
              <label key={idx} className="flex items-center gap-3 cursor-pointer">
                <input type="checkbox" defaultChecked className="w-5 h-5 text-purple-600 rounded" />
                <span className="text-gray-700">{pref}</span>
              </label>
            ))}
          </div>
        </div>
      </div>
    </div>
  );

  // Page: Cart
  const CartPage = () => {
    const total = cart.reduce((sum, item) => sum + item.price, 0);

    return (
      <div className="min-h-screen bg-gray-50 py-8">
        <div className="max-w-5xl mx-auto px-4">
          <h1 className="text-4xl font-bold text-gray-800 mb-8">Mon Panier</h1>

          {cart.length === 0 ? (
            <div className="bg-white rounded-xl shadow-md p-12 text-center">
              <ShoppingCart size={64} className="text-gray-300 mx-auto mb-4" />
              <h3 className="text-xl font-bold text-gray-800 mb-2">Votre panier est vide</h3>
              <p className="text-gray-600 mb-6">Ajoutez des vins pour commencer vos achats</p>
              <button onClick={() => navigate('catalog')} className="bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-lg font-semibold transition">
                Voir le catalogue
              </button>
            </div>
          ) : (
            <div className="grid md:grid-cols-3 gap-6">
              <div className="md:col-span-2 space-y-4">
                {cart.map(item => (
                  <div key={item.cartId} className="bg-white rounded-xl shadow-md p-6 flex gap-4">
                    <div className="bg-gradient-to-br from-purple-100 to-red-100 rounded-lg p-4 flex items-center justify-center">
                      <span className="text-6xl">{item.image}</span>
                    </div>
                    <div className="flex-1">
                      <h3 className="font-bold text-lg mb-1">{item.name}</h3>
                      <p className="text-gray-600 text-sm mb-2">{item.region} • {item.year}</p>
                      <p className="text-2xl font-bold text-purple-900">{item.price}€</p>
                    </div>
                    <button
                      onClick={() => removeFromCart(item.cartId)}
                      className="text-red-500 hover:text-red-700 p-2"
                    >
                      <X size={24} />
                    </button>
                  </div>
                ))}
              </div>

              <div>
                <div className="bg-white rounded-xl shadow-md p-6 sticky top-24">
                  <h3 className="text-xl font-bold text-gray-800 mb-4">Récapitulatif</h3>
                  <div className="space-y-3 mb-6">
                    <div className="flex justify-between">
                      <span className="text-gray-600">Sous-total</span>
                      <span className="font-semibold">{total}€</span>
                    </div>
                    <div className="flex justify-between">
                      <span className="text-gray-600">Livraison</span>
                      <span className="font-semibold">Gratuite</span>
                    </div>
                    <div className="border-t pt-3 flex justify-between">
                      <span className="font-bold text-lg">Total</span>
                      <span className="font-bold text-2xl text-purple-900">{total}€</span>
                    </div>
                  </div>
                  <button className="w-full bg-amber-500 hover:bg-amber-600 text-white py-4 rounded-lg font-bold text-lg transition transform hover:scale-105 shadow-lg mb-3">
                    Commander
                  </button>
                  <button onClick={() => navigate('catalog')} className="w-full bg-gray-200 hover:bg-gray-300 text-gray-800 py-3 rounded-lg font-semibold transition">
                    Continuer mes achats
                  </button>
                </div>
              </div>
            </div>
          )}
        </div>
      </div>
    );
  };

  // Render la page actuelle
  const renderPage = () => {
    switch (currentPage) {
      case 'home': return <HomePage />;
      case 'caves': return <CavesPage />;
      case 'catalog': return <CatalogPage />;
      case 'wine-detail': return <WineDetailPage />;
      case 'my-orders': return <MyOrdersPage />;
      case 'reservations': return <ReservationsPage />;
      case 'favorites': return <FavoritesPage />;
      case 'wine-advice': return <WineAdvicePage />;
      case 'loyalty': return <LoyaltyPage />;
      case 'profile': return <ProfilePage />;
      case 'cart': return <CartPage />;
      default: return <HomePage />;
    }
  };

  return (
    <div className="min-h-screen bg-gray-100">
      <Header />
      <NavMenu />
      {menuOpen && (
        <div
          className="fixed inset-0 bg-black bg-opacity-50 z-40"
          onClick={() => setMenuOpen(false)}
        />
      )}
      <main>{renderPage()}</main>

      {/* Footer */}
      <footer className="bg-gradient-to-r from-gray-900 to-gray-800 text-white py-12 mt-16">
        <div className="max-w-7xl mx-auto px-4">
          <div className="grid grid-cols-1 md:grid-cols-4 gap-8 mb-8">
            <div>
              <div className="flex items-center gap-2 mb-4">
                <Wine size={28} className="text-amber-300" />
                <span className="text-xl font-bold">VinothèqueⓇ</span>
              </div>
              <p className="text-gray-400 text-sm">Votre destination pour les meilleurs vins de France</p>
            </div>
            <div>
              <h4 className="font-bold mb-4">Navigation</h4>
              <div className="space-y-2 text-sm text-gray-400">
                <button onClick={() => navigate('catalog')} className="block hover:text-white">Catalogue</button>
                <button onClick={() => navigate('caves')} className="block hover:text-white">Caves</button>
                <button onClick={() => navigate('wine-advice')} className="block hover:text-white">Conseils</button>
              </div>
            </div>
            <div>
              <h4 className="font-bold mb-4">Mon Compte</h4>
              <div className="space-y-2 text-sm text-gray-400">
                <button onClick={() => navigate('my-orders')} className="block hover:text-white">Commandes</button>
                <button onClick={() => navigate('favorites')} className="block hover:text-white">Favoris</button>
                <button onClick={() => navigate('loyalty')} className="block hover:text-white">Fidélité</button>
              </div>
            </div>
            <div>
              <h4 className="font-bold mb-4">Contact</h4>
              <div className="space-y-2 text-sm text-gray-400">
                <p>📧 contact@vinotheque.fr</p>
                <p>📞 +33 1 23 45 67 89</p>
                <p>📍 Paris, France</p>
              </div>
            </div>
          </div>
          <div className="border-t border-gray-700 pt-8 text-center text-gray-400 text-sm">
            <p>© 2025 Vinothèque. Tous droits réservés. L'abus d'alcool est dangereux pour la santé.</p>
          </div>
        </div>
      </footer>
    </div>
  );
};

export default App;

//// dashboard 1///
<!-- Dashboard Principal -->
<div class="dashboard">
  <!-- ===== SIDEBAR ===== -->
  <aside class="sidebar" [class.active]="sidebarOpen">
    <div class="sidebar-header">
      <div class="logo">
        <span class="logo-icon">🍾</span>
        <span class="logo-text">DrinkStore</span>
      </div>
    </div>

    <!-- Section Navigation Principale -->
    <nav class="nav-section">
      <div class="nav-item"
           [class.active]="currentPage === 'dashboard'"
           (click)="navigateTo('dashboard')">
        <span class="nav-icon">📊</span>
        <span class="nav-text">Tableau de bord</span>
      </div>
      <div class="nav-item"
           [class.active]="currentPage === 'orders'"
           (click)="navigateTo('orders')">
        <span class="nav-icon">🛒</span>
        <span class="nav-text">Mes commandes</span>
        <span class="nav-badge">3</span>
      </div>
      <div class="nav-item"
           [class.active]="currentPage === 'products'"
           (click)="navigateTo('products')">
        <span class="nav-icon">🍷</span>
        <span class="nav-text">Catalogue</span>
      </div>
      <div class="nav-item"
           [class.active]="currentPage === 'favorites'"
           (click)="navigateTo('favorites')">
        <span class="nav-icon">❤️</span>
        <span class="nav-text">Favoris</span>
      </div>
    </nav>

    <div class="divider"></div>

    <!-- Section Mon Compte -->
    <nav class="nav-section">
      <div class="section-label">MON COMPTE</div>
      <div class="nav-item"
           [class.active]="currentPage === 'loyalty'"
           (click)="navigateTo('loyalty')">
        <span class="nav-icon">🎁</span>
        <span class="nav-text">Fidélité</span>
      </div>
      <div class="nav-item"
           [class.active]="currentPage === 'payments'"
           (click)="navigateTo('payments')">
        <span class="nav-icon">💳</span>
        <span class="nav-text">Paiements</span>
      </div>
      <div class="nav-item"
           [class.active]="currentPage === 'addresses'"
           (click)="navigateTo('addresses')">
        <span class="nav-icon">📍</span>
        <span class="nav-text">Adresses</span>
      </div>
      <div class="nav-item"
           [class.active]="currentPage === 'notifications'"
           (click)="navigateTo('notifications')">
        <span class="nav-icon">🔔</span>
        <span class="nav-text">Notifications</span>
        <span class="nav-badge">5</span>
      </div>
    </nav>

    <div class="divider"></div>

    <!-- Section Support -->
    <nav class="nav-section">
      <div class="section-label">SUPPORT</div>
      <div class="nav-item">
        <span class="nav-icon">💬</span>
        <span class="nav-text">Chat Support</span>
      </div>
      <div class="nav-item">
        <span class="nav-icon">❓</span>
        <span class="nav-text">FAQ</span>
      </div>
      <div class="nav-item">
        <span class="nav-icon">⚙️</span>
        <span class="nav-text">Paramètres</span>
      </div>
      <div class="nav-item">
        <span class="nav-icon">🚪</span>
        <span class="nav-text">Déconnexion</span>
      </div>
    </nav>
  </aside>

  <!-- ===== MAIN CONTENT ===== -->
  <main class="main-content">
    <!-- ===== TOP HEADER ===== -->
    <header class="top-header">
      <div class="header-left">
        <!-- Menu Burger Mobile -->
        <div class="burger-menu"
             [class.active]="sidebarOpen"
             (click)="toggleSidebar()">
          <div class="burger-line"></div>
          <div class="burger-line"></div>
          <div class="burger-line"></div>
        </div>

        <!-- Barre de recherche -->
        <div class="search-wrapper">
          <div class="search-bar">
            <input type="text"
                   placeholder="Rechercher un produit, une commande...">
            <button class="search-btn">🔍</button>
          </div>
        </div>
      </div>

      <!-- Droite du header -->
      <div class="header-right">
        <div class="header-icon">
          <span class="icon">🔔</span>
          <span class="badge">{{ notificationCount }}</span>
        </div>

        <div class="user-profile">
          <div class="avatar">👤</div>
          <div class="user-info">
            <div class="user-name">{{ userName }}</div>
            <div class="user-status">{{ userStatus }}</div>
          </div>
        </div>
      </div>
    </header>

    <!-- ===== PAGE: TABLEAU DE BORD ===== -->
    <div *ngIf="currentPage === 'dashboard'" class="page-section active">
      <div class="content-wrapper">
        <!-- En-tête de la page -->
        <div class="page-header">
          <h1 class="page-title">Bienvenue, {{ userName }} ! 👋</h1>
          <p class="page-subtitle">Voici un aperçu de votre activité récente</p>
        </div>

        <!-- Cartes de statistiques -->
        <div class="stats-grid">
          <div *ngFor="let stat of stats"
               [ngClass]="getStatCardClass(stat.color)"
               class="stat-card">
            <div class="stat-icon-wrapper">{{ stat.emoji }}</div>
            <div class="stat-content">
              <div class="stat-label">{{ stat.label }}</div>
              <div class="stat-value">{{ stat.value }}</div>
              <div class="stat-change" [class.positive]="stat.isPositive">
                ↗ {{ stat.change }}
              </div>
            </div>
          </div>
        </div>

        <!-- Actions rapides -->
        <div class="section-header">
          <h2 class="section-title">Actions rapides</h2>
        </div>

        <div class="quick-actions">
          <div class="quick-action" (click)="navigateTo('orders')">
            <div class="quick-action-icon">📦</div>
            <div class="quick-action-text">Suivre commande</div>
          </div>
          <div class="quick-action" (click)="navigateTo('products')">
            <div class="quick-action-icon">🛒</div>
            <div class="quick-action-text">Commander</div>
          </div>
          <div class="quick-action" (click)="navigateTo('loyalty')">
            <div class="quick-action-icon">🎁</div>
            <div class="quick-action-text">Mes récompenses</div>
          </div>
          <div class="quick-action">
            <div class="quick-action-icon">💬</div>
            <div class="quick-action-text">Support</div>
          </div>
        </div>

        <!-- Commandes récentes -->
        <div class="section-header">
          <h2 class="section-title">Commandes récentes</h2>
          <button class="btn btn-secondary" (click)="navigateTo('orders')">
            Voir tout →
          </button>
        </div>

        <div class="orders-grid">
          <div *ngFor="let order of orders.slice(0, 3)" class="order-card">
            <div class="order-image">{{ order.emoji }}</div>
            <div class="order-info">
              <div class="order-title">{{ order.title }}</div>
              <div class="order-details">
                Commande {{ order.id }} • {{ order.quantity }}
                {{ order.quantity === 1 ? 'bouteille' : 'bouteilles' }}
              </div>
              <div class="order-meta">
                <span style="color: var(--text-muted);">📅 {{ order.date }}</span>
                <span class="status-badge" [ngClass]="getStatusClass(order.status)">
                  {{ getStatusText(order.status) }}
                </span>
              </div>
            </div>
            <div class="order-price-section">
              <div class="order-price">{{ order.price | number }} FCFA</div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- ===== PAGE: MES COMMANDES ===== -->
    <div *ngIf="currentPage === 'orders'" class="page-section active">
      <div class="content-wrapper">
        <div class="page-header">
          <h1 class="page-title">Mes Commandes 🛒</h1>
          <p class="page-subtitle">Suivez l'état de vos commandes en temps réel</p>
        </div>

        <div class="orders-grid">
          <div *ngFor="let order of orders" class="order-card">
            <div class="order-image">{{ order.emoji }}</div>
            <div class="order-info">
              <div class="order-title">{{ order.title }}</div>
              <div class="order-details">
                Commande {{ order.id }} • {{ order.quantity }}
                {{ order.quantity === 1 ? 'bouteille' : 'bouteilles' }}
              </div>
              <div class="order-meta">
                <span style="color: var(--text-muted);">📅 {{ order.date }}</span>
                <span class="status-badge" [ngClass]="getStatusClass(order.status)">
                  {{ getStatusText(order.status) }}
                </span>
              </div>
            </div>
            <div class="order-price-section">
              <div class="order-price">{{ order.price | number }} FCFA</div>
              <button *ngIf="order.status === 'delivered'" class="btn btn-secondary" style="font-size: 0.85rem; padding: 0.5rem 1rem;">
                Racheter
              </button>
              <button *ngIf="order.status === 'processing'" class="btn btn-primary" style="font-size: 0.85rem; padding: 0.5rem 1rem;">
                Suivre
              </button>
              <button *ngIf="order.status === 'cancelled'" class="btn btn-secondary" style="font-size: 0.85rem; padding: 0.5rem 1rem;">
                Racheter
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- ===== PAGE: CATALOGUE ===== -->
    <div *ngIf="currentPage === 'products'" class="page-section active">
      <div class="content-wrapper">
        <div class="page-header">
          <h1 class="page-title">Catalogue Produits 🍷</h1>
          <p class="page-subtitle">Découvrez notre sélection de boissons premium</p>
        </div>

        <div class="section-header">
          <div></div>
          <div style="display: flex; gap: 1rem;">
            <button class="btn btn-secondary">🔍 Filtres</button>
            <button class="btn btn-secondary">⬇ Trier</button>
          </div>
        </div>

        <div class="products-grid">
          <div *ngFor="let product of products" class="product-card" (click)="openProductModal(product)">
            <div class="product-image">
              {{ product.emoji }}
              <span *ngIf="product.badge"
                    [class]="'badge-' + product.badge.toLowerCase()"
                    class="product-badge">
                {{ product.badge }}
              </span>
              <button class="favorite-btn"
                      (click)="toggleFavorite(product); $event.stopPropagation()"
                      [class.active]="product.isFavorite">
                {{ product.isFavorite ? '❤️' : '🤍' }}
              </button>
            </div>
            <div class="product-info">
              <h3 class="product-name">{{ product.name }}</h3>
              <p class="product-category">{{ product.category }}</p>
              <div class="product-footer">
                <span class="product-price">{{ product.price | number }} FCFA</span>
                <button class="add-to-cart" (click)="addToCart(product); $event.stopPropagation()">
                  🛒
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- ===== PAGE: FAVORIS ===== -->
    <div *ngIf="currentPage === 'favorites'" class="page-section active">
      <div class="content-wrapper">
        <div class="page-header">
          <h1 class="page-title">Mes Favoris ❤️</h1>
          <p class="page-subtitle">Vos produits préférés en un coup d'œil</p>
        </div>

        <div *ngIf="getFavoriteProducts().length > 0; else noFavorites">
          <div class="section-header">
            <div></div>
            <button class="btn btn-danger">🗑️ Tout supprimer</button>
          </div>

          <div class="products-grid">
            <div *ngFor="let product of getFavoriteProducts()" class="product-card" (click)="openProductModal(product)">
              <div class="product-image">
                {{ product.emoji }}
                <button class="favorite-btn active"
                        (click)="toggleFavorite(product); $event.stopPropagation()">
                  ❤️
                </button>
              </div>
              <div class="product-info">
                <h3 class="product-name">{{ product.name }}</h3>
                <p class="product-category">{{ product.category }}</p>
                <div class="product-footer">
                  <span class="product-price">{{ product.price | number }} FCFA</span>
                  <button class="add-to-cart" (click)="addToCart(product); $event.stopPropagation()">
                    🛒
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>

        <ng-template #noFavorites>
          <div class="empty-state">
            <div class="empty-icon">❤️</div>
            <h2 class="empty-title">Aucun favori</h2>
            <p class="empty-description">
              Ajoutez des produits en favoris pour les retrouver ici
            </p>
            <button class="btn btn-primary" (click)="navigateTo('products')">
              Découvrir les produits
            </button>
          </div>
        </ng-template>
      </div>
    </div>

    <!-- ===== PAGE: FIDÉLITÉ ===== -->
    <div *ngIf="currentPage === 'loyalty'" class="page-section active">
      <div class="content-wrapper">
        <div class="page-header">
          <h1 class="page-title">Programme de Fidélité 🎁</h1>
          <p class="page-subtitle">Gagnez des points et débloquez des récompenses</p>
        </div>

        <div class="loyalty-card">
          <div class="loyalty-header">
            <div class="loyalty-title">Votre Statut</div>
            <div class="loyalty-level">⭐ {{ loyaltyLevel }}</div>
          </div>
          <div class="loyalty-points">
            <div class="points-value">{{ loyaltyPoints | number }}</div>
            <div class="points-label">Points disponibles</div>
          </div>
          <div class="loyalty-progress">
            <div class="progress-bar" [style.width]="'74%'"></div>
          </div>
          <div class="progress-text">
            250 points pour atteindre le niveau PLATINUM 💎
          </div>
        </div>

        <div class="section-header">
          <h2 class="section-title">Récompenses disponibles</h2>
        </div>

        <div class="rewards-grid">
          <div class="reward-card">
            <div class="reward-icon">🎟️</div>
            <div class="reward-title">Réduction 10%</div>
            <div class="reward-description">Sur votre prochaine commande</div>
            <div class="reward-cost">500 pts</div>
            <button class="btn btn-primary" style="width: 100%;">Échanger</button>
          </div>

          <div class="reward-card">
            <div class="reward-icon">🚚</div>
            <div class="reward-title">Livraison Gratuite</div>
            <div class="reward-description">Valable 30 jours</div>
            <div class="reward-cost">300 pts</div>
            <button class="btn btn-primary" style="width: 100%;">Échanger</button>
          </div>

          <div class="reward-card">
            <div class="reward-icon">🍾</div>
            <div class="reward-title">Bouteille Offerte</div>
            <div class="reward-description">Au choix dans une sélection</div>
            <div class="reward-cost">1,000 pts</div>
            <button class="btn btn-primary" style="width: 100%;">Échanger</button>
          </div>

          <div class="reward-card">
            <div class="reward-icon">⭐</div>
            <div class="reward-title">Upgrade VIP</div>
            <div class="reward-description">Accès zone VIP 3 mois</div>
            <div class="reward-cost">2,000 pts</div>
            <button class="btn btn-secondary" style="width: 100%;">Bientôt</button>
          </div>
        </div>
      </div>
    </div>

    <!-- ===== PAGE: PAIEMENTS ===== -->
    <div *ngIf="currentPage === 'payments'" class="page-section active">
      <div class="content-wrapper">
        <div class="page-header">
          <h1 class="page-title">Moyens de Paiement 💳</h1>
          <p class="page-subtitle">Gérez vos cartes bancaires</p>
        </div>

        <div class="section-header">
          <div></div>
          <button class="btn btn-primary">➕ Ajouter une carte</button>
        </div>

        <div class="payment-cards-grid">
          <div class="credit-card visa">
            <div class="card-chip"></div>
            <div class="card-number">•••• •••• •••• 4532</div>
            <div class="card-footer">
              <div class="card-holder-section">
                <div class="card-holder">Titulaire</div>
                <div class="card-name">MARIE DUPONT</div>
              </div>
              <div class="card-expiry-section">
                <div class="card-expiry-label">Expire</div>
                <div class="card-expiry">12/26</div>
              </div>
            </div>
          </div>

          <div class="credit-card mastercard">
            <div class="card-chip"></div>
            <div class="card-number">•••• •••• •••• 8901</div>
            <div class="card-footer">
              <div class="card-holder-section">
                <div class="card-holder">Titulaire</div>
                <div class="card-name">MARIE DUPONT</div>
              </div>
              <div class="card-expiry-section">
                <div class="card-expiry-label">Expire</div>
                <div class="card-expiry">09/25</div>
              </div>
            </div>
          </div>

          <div class="credit-card amex">
            <div class="card-chip"></div>
            <div class="card-number">•••• •••• •••• 2345</div>
            <div class="card-footer">
              <div class="card-holder-section">
                <div class="card-holder">Titulaire</div>
                <div class="card-name">MARIE DUPONT</div>
              </div>
              <div class="card-expiry-section">
                <div class="card-expiry-label">Expire</div>
                <div class="card-expiry">03/27</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- ===== PAGE: ADRESSES ===== -->
    <div *ngIf="currentPage === 'addresses'" class="page-section active">
      <div class="content-wrapper">
        <div class="page-header">
          <h1 class="page-title">Mes Adresses 📍</h1>
          <p class="page-subtitle">Gérez vos adresses de livraison</p>
        </div>

        <div class="section-header">
          <div></div>
          <button class="btn btn-primary">➕ Ajouter une adresse</button>
        </div>

        <div class="addresses-grid">
          <div class="address-card default">
            <div class="address-header">
              <div class="address-type">🏠 Domicile</div>
              <div class="default-badge">Par défaut</div>
            </div>
            <div class="address-details">
              Marie Dupont<br>
              45 Avenue des Champs<br>
              Lekki Phase 1<br>
              Lagos, Nigeria<br>
              📞 +234 801 234 5678
            </div>
            <div class="address-actions">
              <button class="btn btn-secondary" style="flex: 1;">✏️ Modifier</button>
              <button class="btn btn-danger" style="flex: 1;">🗑️ Supprimer</button>
            </div>
          </div>

          <div class="address-card">
            <div class="address-header">
              <div class="address-type">🏢 Bureau</div>
            </div>
            <div class="address-details">
              Marie Dupont<br>
              DrinkStore HQ<br>
              Victoria Island<br>
              Lagos, Nigeria<br>
              📞 +234 809 876 5432
            </div>
            <div class="address-actions">
              <button class="btn btn-secondary" style="flex: 1;">✏️ Modifier</button>
              <button class="btn btn-danger" style="flex: 1;">🗑️ Supprimer</button>
            </div>
          </div>

          <div class="address-card">
            <div class="address-header">
              <div class="address-type">🎁 Autre</div>
            </div>
            <div class="address-details">
              Jean Martin<br>
              78 Banana Island<br>
              Ikoyi<br>
              Lagos, Nigeria<br>
              📞 +234 812 345 6789
            </div>
            <div class="address-actions">
              <button class="btn btn-secondary" style="flex: 1;">✏️ Modifier</button>
              <button class="btn btn-danger" style="flex: 1;">🗑️ Supprimer</button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- ===== PAGE: NOTIFICATIONS ===== -->
    <div *ngIf="currentPage === 'notifications'" class="page-section active">
      <div class="content-wrapper">
        <div class="page-header">
          <h1 class="page-title">Notifications 🔔</h1>
          <p class="page-subtitle">Restez informé de toutes vos activités</p>
        </div>

        <div class="section-header">
          <div></div>
          <button class="btn btn-secondary">✓ Tout marquer comme lu</button>
        </div>

        <div class="notifications-list">
          <div class="notification-item unread">
            <div class="notification-icon success">🎉</div>
            <div class="notification-content">
              <div class="notification-title">Récompense débloquée !</div>
              <div class="notification-message">
                Vous avez gagné 150 points de fidélité pour votre dernière commande.
              </div>
            </div>
            <div class="notification-time">Il y a 2h</div>
          </div>

          <div class="notification-item unread">
            <div class="notification-icon info">📦</div>
            <div class="notification-content">
              <div class="notification-title">Commande livrée</div>
              <div class="notification-message">
                Votre commande #12456 (Moët & Chandon) a été livrée avec succès.
              </div>
            </div>
            <div class="notification-time">Hier</div>
          </div>

          <div class="notification-item">
            <div class="notification-icon warning">🔥</div>
            <div class="notification-content">
              <div class="notification-title">Promotion spéciale</div>
              <div class="notification-message">
                -20% sur tous les champagnes premium ce week-end !
              </div>
            </div>
            <div class="notification-time">Il y a 2 jours</div>
          </div>

          <div class="notification-item">
            <div class="notification-icon success">💳</div>
            <div class="notification-content">
              <div class="notification-title">Paiement confirmé</div>
              <div class="notification-message">
                Votre paiement de 25 000 FCFA pour la commande #12457 a été confirmé.
              </div>
            </div>
            <div class="notification-time">Il y a 3 jours</div>
          </div>

          <div class="notification-item">
            <div class="notification-icon info">🎁</div>
            <div class="notification-content">
              <div class="notification-title">Nouveau niveau atteint</div>
              <div class="notification-message">
                Félicitations ! Vous êtes maintenant membre GOLD.
              </div>
            </div>
            <div class="notification-time">Il y a 1 semaine</div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- ===== CHAT WIDGET ===== -->
  <div class="chat-widget">
    <button class="chat-button">
      💬
      <div class="chat-badge">2</div>
    </button>
  </div>

  <!-- ===== MODAL PRODUIT ===== -->
  <div class="modal" [class.active]="showModal" (click)="closeModal()">
    <div class="modal-content" (click)="$event.stopPropagation()">
      <div class="modal-header">
        <h2 class="modal-title">Détails du produit</h2>
        <button class="close-btn" (click)="closeModal()">×</button>
      </div>

      <div *ngIf="selectedProduct" class="modal-body">
        <div style="text-align: center; margin-bottom: 2rem;">
          <div style="font-size: 8rem; margin-bottom: 1rem;">
            {{ selectedProduct.emoji }}
          </div>
          <h3 style="font-size: 2rem; font-weight: 700; margin-bottom: 0.5rem;">
            {{ selectedProduct.name }}
          </h3>
          <p style="color: var(--text-muted); margin-bottom: 1rem;">
            {{ selectedProduct.category }}
          </p>
          <div style="font-size: 2.5rem; font-weight: 800; color: var(--success); margin-bottom: 2rem;">
            {{ selectedProduct.price | number }} FCFA
          </div>
        </div>

        <div class="form-group">
          <label class="form-label">Description</label>
          <p style="color: var(--text-muted); line-height: 1.8;">
            Produit premium sélectionné avec soin pour vous offrir la meilleure expérience.
            Découvrez ses caractéristiques uniques et saveurs exceptionnelles qui en font un choix privilégié parmi nos clients.
          </p>
        </div>

        <div class="form-group">
          <label class="form-label">Quantité</label>
          <div style="display: flex; gap: 1rem; align-items: center;">
            <button class="btn btn-secondary" (click)="decreaseQuantity()" style="width: 50px; padding: 0.75rem;">
              −
            </button>
            <input type="number"
                   class="form-input"
                   [value]="modalQuantity"
                   style="text-align: center; max-width: 100px;">
            <button class="btn btn-secondary" (click)="increaseQuantity()" style="width: 50px; padding: 0.75rem;">
              +
            </button>
          </div>
        </div>
      </div>

      <div class="modal-footer">
        <button class="btn btn-secondary" (click)="closeModal()">Annuler</button>
        <button class="btn btn-primary" (click)="addProductFromModal()">
          🛒 Ajouter au panier
        </button>
      </div>
    </div>
  </div>
</div>


